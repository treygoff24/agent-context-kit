- Lines 733-737: The guide said top-level `ToolResultSanitizerConfig.mode = "aggressive"` is not a real config mode. In source (`src/sanitizer/tool-result-sanitizer.ts`, `src/guards/session-guard.ts`), `"aggressive"` is part of the type and accepted by config parsing, but it currently behaves the same as `"standard"`; only per-tool aggressive mode halves thresholds. Updated the table entry to match the implementation.
- Lines 762-767: The guide described `detection.enableHashDedup` as an active switch. In source, that field exists in the config type/defaults but is not consulted anywhere; dedup still happens because artifact IDs are content hashes in `src/artifacts/store.ts`. Updated the row to mark the flag as currently unused.
- Lines 771-774: The guide claimed `staleClearing.enabled` and `staleClearing.placeholder` drive context-guard stale-result clearing. In source, those values are merged into config but not read by the guards; the context guard uses hard-coded placeholders/constants in `src/guards/context-guard.ts`. Updated both rows to reflect that they are currently present but unused.
- Lines 854-860: The guide said `metadataLinePrefixes` is used to skip metadata lines during request-snippet extraction. In source, `configureCompactionInstructions()` stores it, but `extractRequestSnippet()` / `isMetadataLine()` do not consult it. Updated that row to call out the current no-op state.
- Lines 906-907: The guide said `artifactStore.enabled` defaults are inferred from the presence of `dir/rootDir`. In source (`resolveArtifactDirSafe` in `src/guards/session-guard.ts`), artifact storage is enabled unless `enabled === false`. Updated the inline comment accordingly.
