- README.md lines 67-69: Recovery example said artifacts were read from `/path/to/artifact.jsonl`, but the current artifact store writes split `.body` and `.meta.json` files (with legacy `.jsonl` only as fallback compatibility). Changed the example path to `/path/to/artifact.body`.
- README.md line 86: Architecture text said stale tool results are cleared "oldest first". In `compactExistingToolResultsInPlace`, messages are sorted by compaction priority, not simple age. Changed this to "lowest-priority first".
- README.md lines 154-159: The example comment implied stubs only appear when output is `>25K chars`. In code, artifact stubbing also triggers for base64-like, high-entropy, and blob-like content below that size threshold. Updated the comment to describe artifact-worthy outputs instead of only `>25K`.
- README.md line 207: The `blob_like` threshold description omitted blob-line/blob-like detection from `isBlobLike(...)`. Updated the table row to mention blob-like content in addition to size, base64-like, and high-entropy triggers.
- README.md line 210: The text said aggressive mode "halves the thresholds". In `classifyToolResultRisk`, aggressive mode halves `inlineSoftChars` and `artifactChars`, but not `giantChars`. Updated the wording to reflect that exactly.
- README.md lines 227-231: The import example included `computeArtifactId`, but the example never uses it and the documented flow only needs `persistToolResultArtifact`. Removed the unused import from the snippet.
