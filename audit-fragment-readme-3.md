- README.md lines 536-542: The `pruneHistoryForContextShare()` example only documented a subset of the returned fields. Updated the comments to include `droppedMessagesList` and `droppedChunks`, which are part of the actual return shape in `src/compaction/utils.ts`.
- README.md lines 673-677: The compaction narrative overstated behavior. The code in `src/guards/context-guard.ts` does not simply clear the oldest tool results, and the files audited here do not implement the claimed retry-timeout escalation flow. Rewrote this section to match the real behavior: lowest-priority non-stub clearing, preemptive overflow signaling, optional use of compaction instructions, summary auditing, and structured fallback.
- README.md lines 689 and 704: The stub example pointed to a `.jsonl` artifact path, but `src/artifacts/store.ts` now stores artifact bodies as `.body` files with separate `.meta.json` metadata. Updated the example recovery path accordingly.
- README.md line 717: The design-philosophy text claimed artifacts are stored as a single JSON envelope. That is only legacy-compatible behavior; current storage is split into UTF-8 `.body` plus `.meta.json` files. Rewrote the paragraph to describe the real on-disk format and note backward-compatible reads.
